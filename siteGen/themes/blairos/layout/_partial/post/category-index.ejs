<% if (post.categories && post.categories.length){ %>
  <div class="article-category-index">
    <%- list_categories(post.categories, {
      show_count: false,
      class: 'article-category-index',
      style: 'none',
      separator: '<span>></span>'
    }) %>
  </div>
<% } %>
